# Project Context

<!-- Why does this project exist? Who is it for? Who owns it?
     Keep concise (~100 lines). Split into separate files only if it grows past that. -->

## Mission

<!-- One paragraph. What outcome does this project create? For whom? -->

Declarative Talos Linux homelab: a machine goes from blank metal to
cluster member with exactly one human act — a wallet signature.
Everything is reconstructible from this git repo plus keys the owner
physically holds. See `../vision.md` for the full narrative.

## Users

Single owner-operator (homelab). Agents are first-class contributors —
see `AGENTS.md` and Taskwarrior (`+repo_5efa11ff`).

## Owners & stakeholders

Repo owner (wallet `0xf568…9406`, SSH key holder). No other stakeholders.

## History (optional)

<!-- Brief — only if context for decisions requires it. -->
